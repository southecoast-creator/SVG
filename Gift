/*
 * LOAD BOOK AUTHOR
 */

const {
  data: bookInfo,
  error: bookInfoError
} =
  await litdomSupabase
    .from("books")
    .select("author_id")
    .eq("id", bookId)
    .single();


/*
 * HANDLE AUTHOR ERROR
 */

if (
  bookInfoError ||
  !bookInfo ||
  !bookInfo.author_id
) {

  console.error(
    "Unable to load book author:",
    bookInfoError
  );

  readerStatus.textContent =
    "Unable to load this book.";

  return;
}


const bookAuthorId =
  bookInfo.author_id;