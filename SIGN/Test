<style>

/* =========================================================
   LITDOM — REFINED STATUS BOOKMARK
========================================================= */

.book-card-header .book-status {

  position: absolute;

  top: 0;
  left: 0;

  z-index: 20;

  display: flex;
  align-items: center;
  justify-content: center;

  min-width: 58px;
  height: 20px;

  margin: 0;
  padding: 0 8px;

  border: none;

  border-radius: 0 0 7px 0;

  font-size: 7px;
  font-weight: 900;

  line-height: 1;

  letter-spacing: .7px;

  text-transform: uppercase;

  white-space: nowrap;

  box-sizing: border-box;

  overflow: hidden;

  box-shadow:
    2px 3px 7px rgba(0,0,0,.28),
    inset 0 1px 0 rgba(255,255,255,.28);

  text-shadow:
    0 1px 1px rgba(255,255,255,.08);
}


/* =========================================================
   SUBTLE GLOSS
========================================================= */

.book-card-header .book-status::before {

  content: "";

  position: absolute;

  left: 0;
  right: 0;
  top: 0;

  height: 45%;

  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,.25),
      rgba(255,255,255,0)
    );

  pointer-events: none;
}


/* =========================================================
   NEAT FOLDED END
========================================================= */

.book-card-header .book-status::after {

  content: "";

  position: absolute;

  right: -3px;
  bottom: -3px;

  width: 5px;
  height: 5px;

  background: rgba(0,0,0,.22);

  clip-path:
    polygon(
      0 0,
      100% 0,
      100% 100%
    );

  opacity: .55;

  filter: blur(.15px);
}


/* =========================================================
   PUBLISHED
========================================================= */

.book-card-header .book-status.status-published {

  background:
    linear-gradient(
      135deg,
      #35C759,
      #27A943
    );

  color: #0B3D1A;
}


/* =========================================================
   APPROVED
========================================================= */

.book-card-header .book-status.status-approved {

  background:
    linear-gradient(
      135deg,
      #35C759,
      #27A943
    );

  color: #0B3D1A;
}


/* =========================================================
   DRAFT
========================================================= */

.book-card-header .book-status.status-draft {

  background:
    linear-gradient(
      135deg,
      #3B82F6,
      #2563C7
    );

  color: #062B68;
}


/* =========================================================
   PENDING
========================================================= */

.book-card-header .book-status.status-pending {

  background:
    linear-gradient(
      135deg,
      #F2C94C,
      #D9AD25
    );

  color: #5A4300;
}


/* =========================================================
   CHANGES REQUESTED
========================================================= */

.book-card-header .book-status.status-changes_requested {

  background:
    linear-gradient(
      135deg,
      #F2994A,
      #D97828
    );

  color: #642B05;
}


/* =========================================================
   REJECTED
========================================================= */

.book-card-header .book-status.status-rejected {

  background:
    linear-gradient(
      135deg,
      #E5484D,
      #C9343A
    );

  color: #5A0D12;
}


/* =========================================================
   MOBILE
========================================================= */

@media (max-width: 600px) {

  .book-card-header .book-status {

    min-width: 52px;

    height: 18px;

    padding: 0 7px;

    font-size: 6.5px;
  }

}

</style>