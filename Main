.led-toggle-button {
  display: inline-flex;
  align-items: center;
  gap: 9px;

  padding: 11px 20px;
  margin-bottom: 20px;

  border: none;
  border-radius: 999px;

  background: linear-gradient(135deg, #59354b, #2d2029);
  color: #f5e3ec;

  font-size: 13px;
  font-weight: 700;
  letter-spacing: .3px;

  cursor: pointer;
  box-shadow: 0 10px 24px rgba(60, 30, 45, .22);
  transition: transform .15s ease, box-shadow .15s ease, background .3s ease;
}

.led-toggle-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 14px 30px rgba(60, 30, 45, .3);
}

.led-toggle-button:active {
  transform: translateY(0);
}

.led-toggle-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #e8b7d0;
  box-shadow: 0 0 8px #e8b7d0;
}

.led-toggle-button.active .led-toggle-dot {
  background: #7be89a;
  box-shadow: 0 0 8px #7be89a;
}